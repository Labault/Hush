# === Base de données ===
POSTGRES_USER=hush
POSTGRES_PASSWORD=CHANGEME_STRONG_RANDOM_PASSWORD_32_CHARS
POSTGRES_DB=hush

# === API ===
JWT_SECRET=CHANGEME_GENERATE_WITH_openssl_rand_base64_48
JWT_EXPIRES_IN=8h
COOKIE_SECRET=CHANGEME_GENERATE_WITH_openssl_rand_base64_48

# === Web ===
# Port HTTP exposé par Nginx vers l'hôte (Caddy reverse-proxy ensuite vers ce port)
WEB_PORT=8080
